# ---------------------------------
# Python 필수 제외
# ---------------------------------
# 가상환경 (Docker 안에서는 시스템 파이썬을 쓰므로 필요 없음)
venv/
.venv/
env/

# 파이썬 캐시 (소스코드 변경 시 충돌 방지)
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/

# 주피터 노트북 체크포인트 (불필요)
.ipynb_checkpoints/

# ---------------------------------
# 데이터 및 모델 (선택 사항)
# ---------------------------------
# 학습용 데이터셋이 너무 크면 제외하고 볼륨으로 연결하는 게 좋음
# data/

# 모델 파일 (.pth)은 보통 복사해야 하므로 제외하지 않음!
# 만약 모델을 외부에서 다운로드 받는 방식이라면 제외해도 됨.
# models/*.pth

# ---------------------------------
# 보안 및 설정
# ---------------------------------
.env
.env.*
.git
.gitignore
Dockerfile
docker-compose.yml
.DS_Store
Thumbs.db

# IDE 설정
.vscode/
.idea/